// Site-wide config knobs.
// Loaded before assets/auth.js on any page that needs them.
//
// Set your Google OAuth 2.0 Web-app client_id here. Get it from:
//   Google Cloud Console → APIs & Services → Credentials → OAuth 2.0 Client IDs
// Then commit + push.

window.__GOOGLE_CLIENT_ID__ = '';   // e.g. "123456-abcdef.apps.googleusercontent.com"
